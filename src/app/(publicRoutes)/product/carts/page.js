export const dynamic = "force-dynamic";
import CartProducts from "@/components/Products/CartProducts/CartProducts.js";
const Carts = () => {
  return (
    <div>
      <CartProducts />
    </div>
  );
};

export default Carts;
